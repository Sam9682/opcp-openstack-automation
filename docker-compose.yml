services:
  skillhub:
    image: nginx:alpine
    container_name: skillhub-opcp-automation
    ports:
      - "8081:80"
    volumes:
      - ./skillhub:/usr/share/nginx/html:ro
      - ./running_skillhub/nginx.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
